import { CensorDirective } from './censor.directive';

describe('CensorDirective', () => {
  it('should create an instance', () => {
    const directive = new CensorDirective();
    expect(directive).toBeTruthy();
  });
});
